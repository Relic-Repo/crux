<nav class="crux__tab-rail" aria-label="Crux panel tabs">
    {{#each tabs as |tab|}}
        <button type="button"
                class="crux__tab {{#if (eq @root.activeTab tab.id)}}is-active{{/if}}"
                data-action="toggleTab"
                data-tab="{{tab.id}}"
                title="{{tab.label}}"
                aria-label="{{tab.label}}"></button>
    {{/each}}
</nav>
